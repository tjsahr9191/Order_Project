# Test configuration using Docker Compose containers
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_project?allowPublicKeyRetrieval=true&useSSL=false&rewriteBatchedStatements=true
    username: order_project_user
    password: password1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  data:
    redis:
      host: localhost
      port: 6379
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
